import { GenoPremiumChrome } from '../brand/graphics';

/** Discover atmosphere — unified premium chrome */
export default function DiscoverChrome() {
  return <GenoPremiumChrome variant="discover" />;
}
