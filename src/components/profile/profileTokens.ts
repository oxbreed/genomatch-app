import { INBOX } from '../inbox/inboxTokens';

/** Profile typography & rhythm */
export const PROFILE = {
  sectionLabelSize: 10,
  sectionTitleSize: 17,
  bodySize: 15,
  metaSize: INBOX.metaSize,
  headerTitleSize: INBOX.headerTitleSize,
  headerSubtitleSize: INBOX.headerSubtitleSize,
  cardPadding: 18,
  sectionGap: 20,
} as const;
